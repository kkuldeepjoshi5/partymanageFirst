package com.eventManagment.Manager;

import com.eventManagment.Utility.Mail;

public class ContactUsManager {

	/*public Mail createMailFromVO(com.eventManagment.vo.MailVO mailVO) {
		Mail mail=new Mail();
		mail.setTo(mailVO.getTo());
		mail.setFrom(mailVO.getSenderEmail());
		mail.setHost(mailVO.getHost());
		mail.setSubject(mailVO.getSubject());
		mail.setText(mailVO.getName()+" "+mailVO.getPhone()+" "+mailVO.getMessage()+" "+mailVO.getSenderEmail());
		return mail;
	}
*/
	
}
